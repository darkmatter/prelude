{
  "name": "acme-typescript-app",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "vitest run",
    "test:unit": "vitest run src/unit",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  }
}
