# Synopsis

Use the shell shortcuts or run the commands directly:

```sh
help       # reprint the welcome banner
menu       # open the interactive command picker
docs       # open this viewer
menu list  # print the command table without a TTY
```

Press a number to jump between pages, `j`/`k` to scroll, and `q` to quit.
