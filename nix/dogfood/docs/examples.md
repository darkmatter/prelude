# Examples

Enter the development shell:

```sh
nix develop
```

Open these docs and jump to the commands page:

```sh
docs
# press 5
```

Run a configured command directly through the menu:

```sh
menu check
```
